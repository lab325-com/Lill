
mutation GardenPlantCareCreate($record: GardenPlantCareCreateInput!) {
	gardenPlantCareCreate(record: $record) {
		id
		name
		count
		period
		sendNotificationAt
		isActive
		nextDate
		status
		CareType {
			id
			name
		}
	}
}
