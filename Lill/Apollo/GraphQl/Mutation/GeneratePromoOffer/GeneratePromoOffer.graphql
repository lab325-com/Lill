mutation GeneratePromoOffer($record: GeneratePromoOfferRecordInput!) {
	generatePromoOffer(record: $record) {
		keyID
        nonce
        timestamp
        signature
        applicationUsername
	}
}
